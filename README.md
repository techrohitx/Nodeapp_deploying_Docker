# Nodeapp_deploying_Docker
Node Proxy Server Application using Docker  This project demonstrates a Node Flask application deployed inside a Docker container with a reverse proxy mechanism to route API requests to an external backend service.
